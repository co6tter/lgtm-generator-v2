# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx,json,css}"
      run: npm run lint
    format:
      glob: "*.{js,ts,jsx,tsx,json,css}"
      run: npm run format
      stage_fixed: true
